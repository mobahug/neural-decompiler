# Neural Decompiler

Neural Decompiler is a mechanistic-interpretability research project investigating whether learned transformer computations can be turned into increasingly complete, causal, and human-understandable explanations.

The project advances through small, reproducible experiments. It separates what was done from what the resulting evidence justifies us in believing, preserves negative and contradictory results, and limits every conclusion to the models, tasks, prompts, and interventions actually tested.

## Research framework

- [Research methodology](docs/RESEARCH_METHODOLOGY.md) — evidence levels, claim governance, falsification rules, and the claim template.
- [Claim registry](research/claims/README.md) — an index of the project's scientific claims. Each claim file is the authoritative record of its evidence and current status.
- [Anomaly registry](research/anomalies/README.md) — a lightweight inbox for unexpected measurements that merit verification, replication, or explanation before they become claims.
- [Causal instrumentation](docs/INSTRUMENTATION.md) — the Pythia-first capture, intervention, decomposition, and provenance contracts used by future experiments.

## Experiments

- [Experiment 001: Capital Recall Logit Lens](experiments/001-capital-recall/README.md) — an observational look at how target-token support changes through Pythia-70M's residual stream.
- [Experiment 002: Held-out Capital-Target Selectivity](experiments/002-capital-selectivity/README.md) — tests whether country context favors the correct capital over balanced incorrect capital-token controls.
- [Experiment 003: Pythia Scale-and-Behavior Comparison](experiments/003-pythia-scale-comparison/README.md) — compares fresh Pythia-70M and Pythia-160M behavior, selectivity, and normalized-depth trajectories under one frozen pipeline.
- [Experiment 004: Prompt Elicitation](experiments/004-prompt-elicitation/README.md) — tests a preregistered prompt intervention selected on development data and evaluated on held-out cases.

Experiments 001–004 are preserved historical observational and infrastructure work. The behavior candidate screen below has since been executed once: it proposes `regular-plural` (count-cued noun number selection in Pythia-70M) as the single target and eliminates `ordinal-suffix`. Experiment 005 ([design, revision 5](docs/superpowers/specs/2026-09-17-experiment-005-regular-plural-mechanism-design.md); [plan](docs/superpowers/plans/2026-09-17-experiment-005-regular-plural-mechanism-plan.md); [experiment](experiments/005-regular-plural-mechanism/README.md)) has been executed to completion: protocol v1's Tier A ended in `NO_COMPACT_MECHANISM` (the compact program missed a template mean), and the protocol v2 continuation's single preregistered confirmation ended in `BEHAVIOR_NOT_REPLICATED` (the reserve nouns failed the behavioral precondition), leaving claim [C002](research/claims/C002-count-cued-noun-number-circuit.md) at `LOCALIZED`. Experiment 006 ([design, revision 4](docs/superpowers/specs/2026-09-18-experiment-006-low-rank-cue-decompilation-design.md); [experiment](experiments/006-low-rank-cue-decompilation/README.md)) ran its Tier A once and stopped at the pre-lock quality gate: a rank ≤ 4 PCA projection of the layer-0 MLP cue encoding cannot represent the cue-to-readout map (normalized leave-one-cue-out RMSE 0.62), so no lock was written and the fresh confirmation set remains unexecuted; Experiment 006 is closed at `QUALITY_GATE_FAILED`. Experiment 007 ([design, revision 3](docs/superpowers/specs/2026-09-18-experiment-007-supervised-cue-subspace-design.md); [plan](docs/superpowers/plans/2026-09-18-experiment-007-supervised-cue-subspace-plan.md); [experiment](experiments/007-supervised-cue-subspace/README.md)) tested a response-supervised shared low-rank cue subspace on the same untouched holdout: Tier A selected rank 1 and passed the pre-lock quality gate (leave-one-cue-out Spearman 0.77, normalized RMSE 0.32; about half the PCA family's error at every rank), and the single preregistered confirmation ended in `PROGRAM_NOT_SUPPORTED`: the rank-1 direction predicts unseen tokens about as well as a full-dimensional ridge map (Y1 MAE 1.07 vs 1.05) but misses the Y1 rank-correlation floor (0.785 vs 0.80) and the confident-sign rule on the singular-selecting determiners `this` and `another`, whose anomalously weak E-patch response neither the selected program nor the tested ridge baseline explains; C002 stays at `LOCALIZED` (P3's correlation floor failed on both fresh sets). Experiment 007 is closed. Experiment 008 ([design, revision 4](docs/superpowers/specs/2026-09-18-experiment-008-cue-suppression-localization-design.md); [experiment](experiments/008-cue-suppression-localization/README.md)), a discovery-only causal trace over the fully exposed pool (40 cue tokens × 18 frames), ran once: its frozen summary is `MIXED`, `every` is an axis artifact of the 007 direction, context gating and late cancellation are rejected, and the consistent finding is at transport — `this`, `a`, `another` still carry about half of the plural cue's encoding-axis signal at the input of `L03.H04`, which attends to the cue normally but outputs 3–19% of the number signal (versus ≈ 100% for numerals and 150–180% for `these`/`those`); the axis component of their encoding alone drives 50–64% of the plural effect while the whole encoding drives none. This transport-selectivity hypothesis is handed to a prospective Experiment 009 ([design, revision 2](docs/superpowers/specs/2026-09-18-experiment-009-head-transport-rule-design.md); [experiment](experiments/009-head-transport-rule/README.md)), whose Tier A ran once: a fixed-normalization linear OV read-out of the residual arriving at `L03.H04` explains the head's number-axis output change over the exposed pool (R² 0.91), so the mechanism level P1 is locked; a weight-only rank-1 transport rule passed its leave-one-cue-out gate while the forty-token contrast rule did not; and the single preregistered confirmation on 23 frozen new cues in 6 new frames ended `HEAD_MECHANISM_CONFIRMED_P1 | TRANSPORT_RULE_FAILED | NOT_LOCKED`: the fixed-normalization linear OV read-out predicted the head's measured transport of every new cue (Spearman 0.96, MAE 0.07 over 138 pairs), while the weight-only rank-1 rule from the encoding missed its rank floor (0.77 vs 0.80) and the frozen category checks (`either`/`neither` are transported mid-way, unlike `this`). The head-level mechanism is confirmed. Experiment 010 ([design, revision 3](docs/superpowers/specs/2026-09-18-experiment-010-read-direction-assembly-design.md); [experiment](experiments/010-read-direction-assembly/README.md)) then attributed the head-readable number signal exactly — no fitting — to the encoding and to every head and MLP of layers 1–2 over 63 exposed cues × 24 frames: the frozen summary is `MIXED` (its relay bound on gross layer change, 0.25, is below every token's baseline of 0.29–0.71), but the ledger shows the opposition inside the encoding itself — for `this`, `a`, `another`, `every` the number-axis part of `E` reads +0.17 to +0.46 of the plural cue's signal through the head's direction and the rest of `E` reads −0.17 to −0.35 in every frame, while layers 1–2 net at most ±0.07 — and the zero-parameter encoding read ranks the 63 tokens' transport with Spearman 0.93. Experiment 011 ([design, revision 2](docs/superpowers/specs/2026-09-19-experiment-011-encoding-read-prospective-design.md); [experiment](experiments/011-encoding-read-prospective/README.md)) then tested that read prospectively: 24 new cue words from five lexical classes and 6 new frames were frozen, the zero-parameter encoding read `g_E` of every new cue was committed from the weights alone (no fresh forward pass), and the single confirmation ended `ENCODING_READ_PREDICTS_TRANSPORT | HEAD_P1_REPLICATED` — the committed reads predicted the head's measured transport of the 24 never-executed cues with Spearman 0.85 and MAE 0.08 (floors 0.80 and 0.248), including the lowest transport for `an` (predicted 0.18, measured 0.30, suppressed by the opposition inside its own encoding), and the linear head read-out replicated on the 142 fresh pairs (Spearman 0.97, MAE 0.08). The support is generalization across this preregistered heterogeneous cue set, not to arbitrary words; the systematic residual is the positive net contribution of layers 1–2 (+0.18 on average), which the encoding read omits by construction. Experiment 011 is closed. Experiment 012 ([design, revision 2](docs/superpowers/specs/2026-09-19-experiment-012-layer-correction-token-local-design.md); [experiment](experiments/012-layer-correction-token-local/README.md)) now tests that residual's computation prospectively: the candidate is the two MLPs of blocks 1–2 evaluated at the cue position on the encoding difference with attention held fixed and exposed template base states (block 1 exact by the parallel residual); on the 87 exposed cues its leave-one-frame-out predictions matched the measured net layer change with Spearman 0.94 and explained variance 0.77, its predictions for 24 new cues in 6 new frames were committed from the weights and locked states alone, and the single confirmation ended `LAYER_CORRECTION_TOKEN_LOCAL_MLP | COMPOSITE_PREDICTS_P1`: the committed corrections predicted the measured net layer-1–2 change of the 24 never-executed cues with Spearman 0.86, MAE 0.06 and explained variance 0.80 (floors 0.80, 0.207, 0.50) — `thy` lowest and negative, `myriad` highest, the quantifier frames' correction far above the others, one sign missed (`which`) — and the composite encoding-read-plus-correction predicted the head's P1 fraction with Spearman 0.92 and MAE 0.06. Stated at its safe strength: the frozen token-local MLP model predicts most of the net correction, and adding it substantially improves the prediction of the representation the head reads (`g_E` alone 0.856 / 0.127 against the P1 fraction; the composite 0.922 / 0.062); the omitted attention terms (block 1's attention change into block 2's input, the heads' own writes) are measured at about 0.05–0.07 each, explain the looser pair level, and — as `thy` shows — can leave the net correction right while the internal MLP/attention attribution is wrong. Experiment 012 is closed. Experiment 013 ([design, revision 2](docs/superpowers/specs/2026-09-19-experiment-013-attention-paths-frozen-pattern-design.md); [experiment](experiments/013-attention-paths-frozen-pattern/README.md)) now asks whether that omitted computation — the frame's own base state and the layer-1–2 heads' value paths with attention patterns held at the reference, an exact per-head split before the approximation — predicts the *residual* the 012 model left: on the 111 exposed cues the frozen-pattern model predicts the residual with Spearman 0.95 and explained variance 0.87 and reproduces `thy`'s MLP/attention compensation without fitting; its predictions for 24 new cues in the 36 exposed frames were written from the weights and locked states alone, the new-frame predictions were digested at confirm before any fresh cue prompt, and the single two-stage confirmation ended `RESIDUAL_PREDICTED_TOKENS | RESIDUAL_PREDICTED_FRAMES_CONDITIONAL | ATTRIBUTION_PREDICTED`: the committed residual predictions matched the measured residual with Spearman 0.88 / R² 0.67 (strict, exposed frames) and 0.92 / 0.75 (conditional, new frames), the predicted template pattern held exactly, the MLP/heads attribution was predicted at the pair level (0.99 / 0.84), and 95% of the 236 preidentified compensation cases carried the predicted signs. The account from the layer-0 encoding to the head's input now closes up to a small attention-pattern-change term (|.| ≈ 0.03 per pair). Experiment 013 is closed. Experiment 014 ([design, revision 2](docs/superpowers/specs/2026-09-19-experiment-014-neuron-1987-feature-design.md); [experiment](experiments/014-neuron-1987-feature/README.md)) descends into the dominant block-2 MLP neuron, 1987: its activation change under a cue swap is predicted by the exact LayerNorm and its own input weights evaluated at the state the decoded circuit predicts, with the number-axis alternative committed for rejection; on the 135 exposed cues the predictor reaches Spearman 0.99, R² 0.99 and balanced firing accuracy 0.99 while the axis-only account fires for almost everything (balanced accuracy 0.61), the measured firing set equals the predicted one (numerals from `three` up, the plural cues, `dozen`, `few`, `many`, `numerous`, `countless`, `myriad`, `various` — no determiner, possessive or adjective), and the single two-stage confirmation ended `NEURON_FEATURE_PREDICTED_TOKENS | NEURON_FEATURE_PREDICTED_FRAMES_CONDITIONAL | AXIS_ONLY_REJECTED`: on 24 never-seen cues the predictor reached Spearman 0.95 / R² 0.97 with balanced firing accuracy 0.92 in the exposed frames and 0.91 / 0.96 / 0.93 in six new frames, the three strong firing predictions (`dozens`, `hundreds`, `thousands`) and all eighteen predicted vetoes held, the committed borderline cases (`infinite`, `millions`, `billions`) came out borderline, and the number-axis alternative was rejected (R² −1.4, balanced accuracy 0.60, firing for 818 of 1152 pairs against 233 measured). The neuron's activation is reconstructed from its own input direction applied to the state the decoded circuit predicts; a plural axis alone cannot account for the activation boundary because off-axis structure vetoes most number-associated cues. Experiment 014 is closed. Experiment 015 ([design, revision 2](docs/superpowers/specs/2026-09-19-experiment-015-attention-pattern-token-local-design.md); [experiment](experiments/015-attention-pattern-token-local/README.md)) writes the one term Experiment 013 still measured from the patched run — the cue-induced change of the layer-1–2 attention patterns at the cue position — as a weight-only query/key program (LayerNorm, projections, partial rotary rotation, scaled dot products, softmax) and separates two levels: the exact recomputation from the frame's actual reference state (Level 1, an identity that reproduces the captured rows and residuals to 1e-5 and counts toward no floor) and the hypothesis, a token-local cue-change model conditional on the reference frame state (Level 0: the cue position's query, key and value changes at the Experiment 012 template-mean bases from the encoding change alone, applied to the frame's reference keys and logit rows). Its predictions for 24 new cues in the 48 exposed frames were locked from the weights and locked states, the new-frame predictions were digested at confirm before any fresh cue prompt, and the single two-stage confirmation ended `PATTERN_CHANGE_PREDICTED_TOKENS | PATTERN_CHANGE_NOT_PREDICTED_FRAMES_CONDITIONAL | AXIS_ONLY_REJECTED`: on the exposed frames the committed rows matched the measured pattern changes with pooled R² 0.91 (layer 1) and 0.70 (layer 2) and their read on the head's direction with Spearman 0.99 / R² 0.90 over the 24 token means; in six new frames the ordering and layer 1 held (0.86; 0.89) but the aggregate missed the explained-variance floors (token means 0.49, layer 2 0.42), one frame — `The agency recruits` — accounting for most of the shortfall; the number-axis alternative was rejected (R² −1.2, entries 0.04; wrong in sign for most cues); and by the predeclared comparator rule the reduced model decodes the query/key interaction beyond the self-logit effect (margins 0.26 / 0.17 over a self-weight-only comparator). Stated at its safe strength: given a frame's reference attention state, a reduced query/key computation driven by the cue's encoding change predicts most of the cue-induced attention-pattern change on unseen cues, and conditionally but incompletely on new frames; the frame still supplies its reference keys, values and logit rows — the change is explained, not the attention state generated. Experiment 015 is closed. Experiment 016 ([design, revision 2](docs/superpowers/specs/2026-09-20-experiment-016-frame-conditioned-qk-design.md); [experiment](experiments/016-frame-conditioned-qk/README.md)) asks what the frame contributes: a design check on all 6180 exposed pairs found that no compact descriptor (the residual's mean and scale, a direction, a shared low-dimensional subspace of frame deviations, a few heads, a few block-1 neurons) recovers the layer-2 change, while three literal reference-state channels do — the frame's own query and key vectors at the cue position as the operands of the self-logit, the frame's LayerNorm scale before and (derived from the predicted change) after the cue change, and block 1's MLP at the frame's own operating point — with the frame residual's direction contributing only a small renormalization remainder. The frame-conditioned model's predictions for 24 new cues in the 54 exposed frames were locked, the twelve new frames' predictions were digested at confirm before any fresh cue prompt, and the single two-stage confirmation ended `FRAME_CHANNELS_PREDICTED_TOKENS | FRAME_CHANNELS_PREDICTED_FRAMES_CONDITIONAL | SCALE_ONLY_REJECTED`: pooled entry R² 0.994 / 0.988 (layers 1 / 2) and token means Spearman 0.998 / R² 0.994 in the exposed frames, 0.995 / 0.986 and 1.000 / 0.996 in the twelve new frames with every frame above the preregistered per-frame guard (lowest 0.933, a frame where Experiment 015's model reaches 0.51), the scale-only alternative rejected (0.672, 0.316 below), and the ablations ordered as predeclared (operands 0.28 > operating point 0.10 > scale 0.04). Stated at its safe strength: three structured classes of reference-state information suffice for the token-local cue change to predict the attention-pattern change nearly exactly on unseen cues and frames; not three numbers, and not attention from text. Experiment 016 is closed. Experiment 017 ([design, revision 2](docs/superpowers/specs/2026-09-20-experiment-017-transport-head-pattern-design.md); [experiment](experiments/017-transport-head-pattern/README.md)) closes the chain at the transport head itself, which had been described with a frozen attention pattern since Experiments 009, 011 and 013: it predicts `L03.H04`'s own attention-row change at the transport position and its end-to-end output change `ΔT` from the decoded upstream state alone — the encoding change, Experiment 016's frame-conditioned token-local changes of layers 1–2, block 1's and block 2's MLPs at the frame's operating points, one propagation step where the transport position follows the cue, and the head's own query/key/value changes reduced as Experiment 016 reduced layers 1–2 — never from a fresh layer-3 residual or head state, with the exact chain from the reference state kept as an identity (I4–I7, 1e-5) that enters no floor. The predictions for 24 new cues in the 66 exposed frames were locked, the twelve new frames' predictions were digested at confirm before any fresh cue prompt, and the single two-stage confirmation ended `HEAD_PATTERN_PREDICTED_TOKENS | HEAD_PATTERN_PREDICTED_FRAMES_CONDITIONAL | FROZEN_PATTERN_NOT_REJECTED`: head-row entry R² 0.997 / 0.998 (exposed / new frames), the pattern term `Π` on token means R² 1.000 / 1.000, `ΔT` on pairs 0.999 / 1.000, every new frame above the per-frame guard (lowest 0.986), while the frozen-pattern alternative — the reference row carrying the predicted value change — reached R² 0.958 on the pooled cue-final pairs with a gap of 0.042 to Level 0, short of the preregistered rejection (below 0.95 and a gap of at least 0.05). The reading fixed before the run applies: the head's pattern change was predicted, but the fresh set did not establish that modelling it explicitly was necessary relative to the frozen-pattern account; descriptively the term's share of the `ΔT` variance is 0.04 on these cues against 0.08 on the exposed pool, and within each template the frozen pattern is far from complete (0.91 cardinal, 0.88 quantifier over both sets; 0.59 and 0.42 in the new frames), the pooled value being carried by the between-template spread of `ΔT` — recorded, not applied. The ablations hold their predeclared order (the head's frame channels cost 0.17 of row R², block 2's operating point 0.05), and block 2's operating point closes the whole of the decoded-`c_L` gap Experiment 016 left (R² 1.000 against 0.81). Stated at its safe strength: the cue-to-transport chain is now written end to end as a composed weight-only program whose every head input was predicted, not measured; whether the pattern term must be modelled is, on these cues, undecided. Experiment 017 is closed. Experiment 018 ([design, revision 2](docs/superpowers/specs/2026-09-20-experiment-018-block2-concentration-design.md); [plan](docs/superpowers/plans/2026-09-21-experiment-018-block2-concentration-plan.md); [experiment](experiments/018-block2-concentration/README.md)) asks whether that program's least compact object — channel D, block 2's MLP at the frame's own operating point (2048 reference pre-activations per changed position) — is carried by a fixed subset of neurons: it replaces channel D by its masked form (a locked subset at the frame's operating point, every other neuron at the template base), chooses the subset once by a frozen read-unit rule on the 9636 licensed exposed pairs from the *predicted* arriving change (no measured quantity), locks the ladder `S_0 … S_2048`, three seeded random 256-subsets and the 256 lowest-ranked as controls, and scores the closure fraction `κ` — the share of channel D's contribution a rung recovers, unclipped, undefined below a 0.05 gap — with the single-neuron account committed for rejection and the within-frame pattern-term question of Experiment 017 carried as a preregistered secondary outcome from the reference rung. The 1872 predictions for 24 new cues in the 78 exposed frames were locked (the reference rung reproducing Experiment 017's Level 0 to 1e-14), the twelve new frames' predictions were digested at confirm before any fresh cue prompt, and the single two-stage confirmation ended `CHANNEL_D_CONCENTRATED_TOKENS | CHANNEL_D_NOT_CONCENTRATED_FRAMES_CONDITIONAL | SINGLE_NEURON_REJECTED | PATTERN_TERM_NOT_ESTABLISHED_WITHIN_FRAMES`: on the unseen cues the locked 256 neurons recover `κ_{c_L}` 0.863 and `κ_Π` 0.724 (floors 0.70 / 0.50; random controls 0.23–0.32, the rung curve `S_4` 0.60, `S_64` 0.72, `S_256` 0.86, `S_1024` 0.98, the head's row dependence diffuse at 0.22 as predeclared); on the twelve new frames the pooled closure (0.843 / 0.798) and both template-family guards pass, but the no-harm guard fails in one frame — `The atlas maps {cue}`, where the locked subset reaches `c_L` R² 0.745 against 0.847 at the template base while the full channel reaches 0.999 and the frame's own top 256 reaches 0.924 — so the frame-conditional label is negative as preregistered; the single-neuron account is rejected (neuron 1987 first in the ranking but `κ` 0.219 against 0.860, gap 0.641); and three new cardinal frames keep the frozen pattern at or above the 0.90 ceiling (0.951, 0.927, 0.913), so the within-frame pattern-term condition is not established on this sample and Experiment 017's reading stands. Stated at its safe strength: channel D is prospectively concentrated in a distributed subset of block-2 neurons across unseen cues, but the exact concentration is not fully invariant across new frames; a single-neuron explanation is rejected. Descriptively, neuron 1102 overtakes 1987 in the new quantifier frames — which members of the population carry the computation appears to depend on the frame, the natural question for Experiment 019. Experiment 018 is closed. Experiment 019 ([design, revision 3](docs/superpowers/specs/2026-09-21-experiment-019-block2-routing-design.md); [plan](docs/superpowers/plans/2026-09-21-experiment-019-block2-routing-plan.md); [experiment](experiments/019-block2-routing/README.md)) asks that question prospectively: is the frame-specific membership of block 2's recovery subset predicted from the frame's reference state before any fresh cue runs? Experiment 018's masked chain takes one mask per changed position, and a nested ladder of selectors at size 64 — the matched global `S'_64` as the comparator, the template ranking `T_64`, the full evaluation `E_64(f, p)` (the template's exposed cues' *predicted* effects at the frame's own reference state) and the compact operating-point-plus-drive rule `G_64(f, p)` (the frame's reference pre-activations plus a frozen per-template three-quantile drive profile through the exact GELU), with the inherited 018 subset, seeded random subsets and single neurons as controls — is scored by the closure fraction `κ` of channel D's contribution to the transport read, every list computed from locked states, weights and the exposed cues' `ΔE` and committed before the fresh prompts it is scored on; the measured-effect ranking and a leave-one-cue-out greedy fit enter no selector and serve only as the membership target and as a cross-validated headroom witness. The 2160 predictions for 24 new cues in the 90 exposed frames were locked, the eighteen new frames' lists and predictions were digested at confirm before any fresh cue prompt, and the single two-stage confirmation ended `ROUTING_PREDICTED_TOKENS | ROUTING_PREDICTED_FRAMES_CONDITIONAL | OPERATING_POINT_PLUS_DRIVE_RULE_SUFFICIENT | TEMPLATE_FAMILY_INSUFFICIENT | MEMBERSHIP_PREDICTED`: on the unseen cues the frame's own `E_64` recovers `κ_{c_L}` 0.813 against 0.722 for the global `S'_64` (`Δκ` 0.090, floor 0.05; above the global subset in 64 of 90 frames; both family splits above 0.02), on the eighteen new frames 0.793 against 0.649 (`Δκ` 0.144; 14 of 18 frames); the compact rule carries `ρ_64` 0.862 / 1.136 / 0.913 of that gain (Y1 / new frames / pooled; floors 0.50 / 0.50 / 0.60 — unclipped, the value above one meaning that on the new frames `G_64`'s gain over the same baseline exceeded `E_64`'s, not an explanatory completeness above the whole); the frame-specific evaluation exceeds the template ranking by 0.068 pooled and 0.080 on the new frames (margin 0.05), and the predicted `E_64` shares 0.867 of its members with the measured-effect ranking's top 64 on both sets against 0.410 / 0.375 for the global subset. The frozen interpretation table gives Account 2, compact form: the frame's operating point, with a frozen per-template drive profile, routes channel D — a frame's participating neurons are named from its reference pre-activations and the locked drive tables, blind to the fresh cues, in exposed and new frames, and the template ranking does not suffice; the decoded program gains the routing rule `reference state → operating point (+ the cue population's typical drives) → which of the 2048 neurons carry the correction → the transport read`. Retained limits: `E_64` loses more than 0.05 R² to the global subset in 7 of 90 exposed and 2 of 18 new frames, so the result is an aggregate routing result under the preregistered majority and family guards, not a per-frame rule; the cross-validated witness `O*` (never an oracle, ceiling or upper bound) demonstrates further transferable selectable headroom `H*_64` 0.210 / 0.241 of which `E` captures 0.43 / 0.60; the descriptive expectation that the gain over the inherited 018 subset at least equals the gain over `S'_64` failed on the new frames by 0.0008 (0.1431 against 0.1439), recorded with no outcome force; the pronouns' near-zero transport reads and the other token and frame heterogeneity remain descriptive. Stated at its safe strength: a fixed 64-neuron population is not the whole story — frame-specific routing within block 2 is prospectively predictable from the frame's reference state, a compact operating-point-plus-typical-drive rule carries most of that routing gain on unseen cues and unseen frames while template-family identity alone is insufficient, and the predicted selector substantially recovers the neurons ranked highest by the fresh measured effects; the rule is population-level rather than universal per frame and does not exhaust the headroom the witness demonstrates. Experiment 019 is closed. Experiment 020 ([design, revision 3](docs/superpowers/specs/2026-09-22-experiment-020-readout-decompilation-design.md); [plan](docs/superpowers/plans/2026-09-22-experiment-020-readout-decompilation-plan.md); [experiment](experiments/020-readout-decompilation/README.md)) carried the decoded chain through blocks 3–5, `LN_final` and the weight-only noun read to the model's own singular-versus-plural logit contrast. It is closed at Tier A: exploration completed at `ce3766b` (after a recorded Level-1 incident at `abf3df9`, diagnosed as the pinned float32 forward's attention-score precision floor, and an authorized identity-tolerance amendment from 1e-3 to 7e-3); confirmation was intentionally not run because the corrected block-5 parallel-residual implementation materially changed the exposed calibration on which the preregistered floors had been based — the scratch pass that set them fed block 5's MLP the post-attention residual. All six identities passed; the corrected exposed flattened `R²` is 0.689 (scratch 0.848; cardinal 0.853, quantifier 0.843, coordinated-adjective 0.273), six of the twelve floor conditions sit below threshold on the exposed pool itself, and the same program fed the measured `Δx3` reaches 0.976, so the downstream readout is decoded almost exactly and the remaining error is the inherited upstream prediction of `Δx3`. The 020 floors were not changed retrospectively, no Y1/Y2/Y3 label is assigned, C002 is unchanged, and the never-executed confirmation set (`e098e2b4…`) is reserved for the next protocol version.

## Behavior candidate screening

The candidate-selection study is frozen in [the screening design](docs/superpowers/specs/2026-09-16-behavior-candidate-screening-design.md) and its [implementation plan](docs/superpowers/plans/2026-09-16-behavior-candidate-screening.md). It screens exactly two retained candidates, `regular-plural` and `ordinal-suffix`, against fixed behavioral gates, trivial baselines, an exploratory causal compactness probe, and a finalist prior-art audit. It can return zero or one proposed target. It is candidate selection, not Experiment 005, and it creates no Experiment 005 directory, claim, or preregistration.

The committed manifest `screening/behavior-candidates/manifest-v1.json` is the sole authority for every case, split, tokenization decision, control, seed, and threshold. The runner exposes four phases and no flag that selects a candidate, model scale, threshold, or template:

```bash
uv run python screening/behavior-candidates/run.py validate
uv run python screening/behavior-candidates/run.py behavioral
uv run python screening/behavior-candidates/run.py compactness
uv run python screening/behavior-candidates/run.py report
```

- `validate` checks the manifest digest, counts, split disjointness, and single-token development cases without loading a model.
- `behavioral` runs both candidates on pinned Pythia-70M and repeats the byte-identical protocol on pinned Pythia-160M only when zero candidates pass every 70M gate.
- `compactness` probes only behavioral passers with development data; holdout and future-reserve cases are never executed during selection.
- `report` renders the candidate matrix; `report --audit-json JSON` records the finalist prior-art audits (write-once per candidate) and the final zero/one-target decision.

The screen was executed once on 2026-09-17; the outcome, evidence copy, and finalist audit are recorded in [`screening/behavior-candidates/README.md`](screening/behavior-candidates/README.md): `regular-plural` (count-cued noun number selection) is the single proposed target and `ordinal-suffix` was eliminated. Experiment 005 has run once under protocols v1 and v2; see its README for the recorded outcomes.

Scientific phases require a clean, committed tree and run once. `--resume` continues only an interrupted, provenance-identical run. A completed screen is rerun only through `--incident-note TRACKED_PATH`, which invalidates the whole affected model screen for every candidate after a committed software fix. Generated outputs live under `outputs/behavior-candidate-screening/` (`results.json`, `report.md`) and are not committed.

## Causal instrumentation

The Phase 1 instrumentation layer provides pinned Pythia loading, canonical TransformerBridge component resolution, selective activation capture, exact zero/replacement interventions, Pythia-specific decomposition checks, task-independent behavior declarations, and reproducible provenance records.

Its purpose is to support progressively stronger causal accounts of learned computation. A captured activation, a decomposition identity, or a behavioral change after ablation is not by itself a mechanistic explanation. Future experiments must preregister controls, competing explanations, predicted intervention outcomes, and success criteria.

The test suite is offline and organised in tiers (`tests/conftest.py` classifies every test by its path; `tests/test_tiers.py` checks the partition on every run). A bare `pytest` runs tier A only; the historical runner tests never run unless asked for.

| tier | selection | when | command |
|---|---|---|---|
| A | unit tests that are not slow | the normal edit/test loop (< 1 min) | `uv run pytest` |
| B | every unit test (slow ones included) plus the current experiment's runner test | while the current experiment changes (minutes) | `uv run pytest --tier B` |
| C | tier B plus the pinned-model contract | the gate before `explore`, `lock` and `confirm`, on a clean tree at the gated commit | `HF_HUB_OFFLINE=1 uv run pytest --tier C` |
| D | the closed experiments' runner tests (fake replays; hours) | when shared source, the fakes or the locked dependencies change; overnight; never a routine gate | `uv run pytest --tier D` |
| all | everything (the pinned-model contract still needs its opt-in variable) | periodic | `uv run pytest --tier all` |

`--tier C` sets `NEURAL_DECOMPILER_RUN_PYTHIA_SMOKE=1` itself; the contract tests skip in every other tier unless that variable is set. The experiment under development is named by `CURRENT_EXPERIMENT` in `tests/conftest.py` (or the `NEURAL_DECOMPILER_CURRENT_EXPERIMENT` environment variable); it is unset while no experiment is open, and every `tests/test_experiment_*` file is then historical. Explicit selections — paths, node ids, `-m`, `-k` — switch the default tier off, so `uv run pytest tests/test_experiment_017_runner.py` runs that file; `--tier` always applies and intersects with them. Process-parallel runs use the optional `parallel` group (`uv sync --group parallel`): `OMP_NUM_THREADS=2 uv run pytest --tier D -n 3 --dist loadfile` keeps each test module, and so each module-scoped fake world, on one worker; the pinned-model contract should run serially. Markers: `unit`, `slow`, `runner`, `current`, `historical`, `pythia_smoke`, and `protocol` (reserved for the explicit protocol-guarantee tests of new experiments).

## Setup

The project uses [uv](https://docs.astral.sh/uv/) to select Python 3.12 and install the locked dependencies:

```bash
uv sync
```

Model weights are downloaded only when an experiment is run. Hugging Face and TransformerLens keep them in their normal user cache outside this repository.
